# 案例编写模板

在为 Skill 的 `examples/` 目录创建新案例时，请使用以下骨架快速起步。

---

## {案例名称}

### 背景概述

> 简要说明这个案例的背景：产品/场景是什么？为什么它是一个有代表性的好案例？

### 输入

```
{粘贴实际的原始输入数据}
{如果输入来自上游 Skill，说明来源}
```

### 输出

```
{完整的、高质量的标准输出}
{必须展示最终交付物的完整形态，不要省略任何部分}
```

### 关键策略标注

| 输出片段 | 使用的策略/技巧 | 为什么有效 |
|---------|-------------|-----------|
| "{引用输出中的关键句}" | {策略名称} | {说明原因} |
| "{引用输出中的另一句}" | {策略名称} | {说明原因} |

### 可复用要素

- **脚本骨架**：{本案例的叙事框架可直接套用到 ___ 类产品}
- **关键话术**：{列出可直接替换关键词后复用的句式}
- **节奏模板**：{时间分配参考，如 Hook 3s + Agitate 7s + ...}
